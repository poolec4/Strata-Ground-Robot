close all
clear all
clc

kp = 6;
ka = 30;
kb = -25;

rob_pos = [-5, 0];
rob_th = 0;
goal_pos = [0, 0];
goal_th = 0;

dt = 0.1;
t = 0:dt:20;

for i = 1:length(t)
    
    dx = goal_pos(1) - rob_pos(i,1);
    dy = goal_pos(2) - rob_pos(i,2);
    
    p = sqrt(dx^2 + dy^2);
    a = -rob_th + atan2(dy,dx);
    b = -rob_th - a;
    
    v = kp*p;
    omega = ka*a + kb*b;
    
    rob_pos(i+1,1) = roc_pos(i,:) + dt*v
end